# Python dependencies for the FlowGAN manuscript reproduction package.
#
# These versions are pinned deliberately: the shipped figures, tables and the
# executed revision_report.ipynb were generated with exactly this set, and it was
# verified to reproduce every table and every number in the notebook. Pinning is
# what makes that claim checkable later, once newer releases have moved on.
#
# Verified with Python 3.11. Install with:
#
#     conda create -n FlowGAN_repro python=3.11
#     conda activate FlowGAN_repro
#     pip install -r requirements.txt
#
# The analysis is not sensitive to these exact versions — it was also checked
# against considerably older releases and produced identical values — so relaxing
# a pin is usually safe if your environment requires it.

numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
matplotlib==3.11.0
seaborn==0.13.2
scikit-image==0.26.0        # SSIM / image quality metrics (utils.py)
statsmodels==0.14.6         # McNemar's test, FDR correction
statannotations==0.7.2      # significance annotations on boxplots (02)
nibabel==5.4.2              # NIfTI I/O (only needed to rebuild pickles from raw imaging)
tqdm==4.68.4
openpyxl==3.1.5             # read/write .xlsx (clinical metadata, output tables, regional_data.xlsx, source_data/)

# To open and re-run revision_report.ipynb:
jupyter==1.1.1
nbformat==5.10.4
nbconvert==7.17.1

# OPTIONAL — only for rendering the cortical-surface quadrant maps documented
# (but not executed) in the notebook. Not required to reproduce any manuscript number.
# atlaspy
